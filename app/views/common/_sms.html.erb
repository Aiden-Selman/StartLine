require 'twilio-ruby'

client = Twilio::REST::Client.new(
  ENV['TWILIO_ACCOUNT_SID'],
  ENV['TWILIO_AUTH_TOKEN']
)

reciever_phone_number = params[:phone_number]

client.messages.create(
  from: +13212521946,
  to:   ,
  body: 'Hello from StartLine!'
)