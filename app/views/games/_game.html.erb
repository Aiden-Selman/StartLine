<% if cart.include?(game) %>
  <%= button_to cart_path(game), method: :delete do %>
    ❌ Remove From Cart
  <% end %>
<% else %>
  <%= form_with  url: cart_index_path, method: :post do %>
    <%= hidden_field_tag(:id, game.id) %>
    <%= button_tag 'Add to Cart' %>
  <% end %>
<% end %>